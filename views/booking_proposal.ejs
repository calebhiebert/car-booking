<!DOCTYPE html>
<html lang="en">
<head>
    <title>Car Booking</title>
    <% include head.ejs %>
</head>
<body>
<div class="container mt-3">
    <div class="card card-inverse card-success" style="color: white;">
        <div class="text-center">
            <h1>Success!</h1>
            <strong>How does this look?</strong>
        </div>
        <table class="table table-hover mb-0 mt-2">
            <tbody>
                <tr>
                    <th scope="row">Name</th>
                    <td><%= booking.name %></td>
                </tr>
                <tr>
                    <th scope="row">Function</th>
                    <td><%= booking.function %></td>
                </tr>
                <tr>
                    <th scope="row"># of People</th>
                    <td><%= booking.numPeople %></td>
                </tr>
                <tr>
                    <th scope="row">Start Time</th>
                    <td><%= moment(booking.unixStartTime * 1000).format('LLL') %></td>
                </tr>
                <tr>
                    <th scope="row">Return Time</th>
                    <td><%= moment(booking.unixReturnTime * 1000).format('LLL') %></td>
                </tr>
                <tr>
                    <th scope="row">Reason</th>
                    <td><%= booking.reason %></td>
                </tr>
                <% if (booking.notes.length > 0) { %>
                    <tr>
                        <th scope="row">Notes</th>
                        <td><%= booking.notes %></td>
                    </tr>
                <% } %>
                <tr>
                    <th scope="row">Vehicle</th>
                    <td><%= booking.vehicle.name + (' (Vehicle ID: ' + booking.vehicle.vid + ', Seats ' + booking.vehicle.numSeats + ')') %></td>
                </tr>
                <% if (booking.vehicle.notes.length > 0) { %>
                    <tr>
                        <th scope="row">Vehicle Notes</th>
                        <td><%= booking.vehicle.notes %></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    <form action="/booking_proposal" method="get">
        <div class="card mt-3">
            <div class="card-block">
                    <fieldset class="form-group">
                        <label for="email">Please enter your email so we can send you a calendar invite</label>
                        <input type="email" id="email" name="email" class="form-control" required>
                    </fieldset>
            </div>
        </div>
        <button type="submit" class="btn btn-primary mt-3">Accept Booking</button>
    </form>
</div>
<% include scripts.ejs %>
</body>
</html>